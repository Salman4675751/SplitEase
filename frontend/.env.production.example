# Frontend production environment template — copy to .env.production before building
# Used by Vite at build time via import.meta.env.VITE_API_URL

# Public URL where the backend API is reachable
VITE_API_URL=https://api.itcentralpark.com/api
